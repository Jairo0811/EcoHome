import {useEffect,useState} from 'react';import {api} from '../api/http';import type {ReportOverview} from '../types/reports';export function useReports(){const [data,setData]=useState<ReportOverview|null>(null),[error,setError]=useState<string|null>(null);useEffect(()=>{let active=true;api.getReportOverview(30).then(x=>{if(active){setData(x);setError(null)}}).catch(e=>{if(active)setError(e instanceof Error?e.message:'No se pudo cargar el reporte.')});return()=>{active=false}},[]);return{data,error}}
