import {useReports} from '../hooks/useReports';
const max=(v:number[])=>Math.max(...v,1);export function AdvancedAnalytics(){const {data,error}=useReports();if(error)return <article className="panel"><p className="subtitle">{error}</p></article>;if(!data)return <article className="panel"><p className="subtitle">Cargando analítica…</p></article>;const energy=data.trends.ENERGY_KWH;const m=max(energy.map(x=>x.total));return <article className="panel consumption-panel"><div className="panel-heading"><div><p className="eyebrow">Analítica · 30 días</p><h3>Rendimiento del hogar</h3></div><span className="period-chip">{data.periodDays} días</span></div><div className="stats-grid"><div><strong>{data.alerts.open}</strong><span className="subtitle"> alertas abiertas</span></div><div><strong>{data.securityEvents}</strong><span className="subtitle"> eventos seguridad</span></div><div><strong>{data.automationExecutions}</strong><span className="subtitle"> automatizaciones</span></div></div><div className="resource-list"><strong>Tendencia de energía</strong>{energy.length===0?<p className="subtitle">Sin datos históricos.</p>:<div style={{display:'flex',alignItems:'end',gap:4,height:90}}>{energy.map(p=><span key={p.date} title={`${p.date}: ${p.total} kWh`} style={{display:'block',flex:1,minWidth:4,height:`${Math.max(p.total/m*100,4)}%`,background:'currentColor',opacity:.55,borderRadius:4}}/>)}</div>}</div></article>}
