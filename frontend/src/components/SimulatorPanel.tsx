import {useState} from 'react';import {api} from '../api/http';export function SimulatorPanel(){const [status,setStatus]=useState('Listo para generar datos de prueba.');async function run(){setStatus('Simulando…');try{const r=await api.runSimulation(10);setStatus(`${r.telemetryCreated} lecturas sintéticas generadas en el hogar #${r.home}.`)}catch(e){setStatus(e instanceof Error?e.message:'La simulación falló.')}}return <article className="panel"><div className="panel-heading"><div><p className="eyebrow">Laboratorio IoT</p><h3>Simulador de telemetría</h3></div><button className="primary-button" onClick={()=>void run()}>Simular 10 ciclos</button></div><p className="subtitle">{status}</p></article>}
