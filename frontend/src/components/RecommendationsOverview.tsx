import {useRecommendations} from '../hooks/useRecommendations';export function RecommendationsOverview(){const {items,dismiss,apply}=useRecommendations();return <article className="panel devices-panel"><div className="panel-heading"><div><p className="eyebrow">EcoHome Insights</p><h3>Recomendaciones inteligentes</h3></div><span className="device-count">{items.length}</span></div>{items.length===0?<div className="empty-state"><strong>Sin recomendaciones pendientes</strong><p>Tu hogar no requiere ajustes destacados en este momento.</p></div>:<div className="device-list">{items.slice(0,5).map(x=><div className="device-row" key={x.id}><div className="device-icon">{x.priority==='HIGH'?'!':'✦'}</div><div className="device-copy"><strong>{x.title}</strong><span>{x.description}{x.estimated_savings_percent?` · ahorro estimado ${x.estimated_savings_percent}%`:''}</span></div><div className="hero-actions"><button className="secondary-button" onClick={()=>void dismiss(x.id)}>Descartar</button><button className="primary-button" onClick={()=>void apply(x.id)}>Aplicada</button></div></div>)}</div>}</article>}
